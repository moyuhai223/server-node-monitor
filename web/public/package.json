{
  "name": "snm-public-dashboard",
  "private": true,
  "version": "1.0.0",
  "description": "Server Node Monitor public dashboard (static). npm is only used to fetch the SignalR browser bundles copied by scripts/build-web.sh.",
  "devDependencies": {
    "@microsoft/signalr": "10.0.11",
    "@microsoft/signalr-protocol-msgpack": "10.0.11"
  }
}
